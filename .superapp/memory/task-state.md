# Task state

Goal: initial Sales Ping app — home dashboard + navigation skeleton.

Done:
- Theme, models, sample store, tab navigation, home dashboard (hero revenue card,
  stat tiles, weekly chart, processor strip, recent pings + sale detail drill-down),
  placeholder tabs, themed launch screen. Build succeeded, ran on simulator.

Next likely steps:
1. History tab: full searchable/filterable sales list.
2. Connect tab: processor connect flows (OAuth/API key) — needs backend for secrets.
3. Settings: ping sound picker (cha-ching default) + notification text template builder.
4. Push notifications + backend (Supabase) for webhooks; RevenueCat paywall.
